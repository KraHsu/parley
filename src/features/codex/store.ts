// Compatibility import for existing extensions; state lives in the generic chat store.
export { useChatStore as useCodexStore } from '../chat/store'
export type { Pane, Message, Conversation, VocabularyAnswerTarget } from '../chat/types'
