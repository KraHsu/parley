# 词句学习实现记录

目标：完整实现 [词句学习开发计划](VOCABULARY_PLAN.md) 的 v0.2 与 v0.3，当前仍在开发与验证中，不能视为发布完成。

## 已落地的代码

- schema 002：词条、来源、草稿、写入幂等记录和导入映射；schema 003：标签、独立方向的复习卡与评分历史。
- Rust 词句 CRUD、搜索/筛选/分页、回收站、冲突保护、Unicode 选区、原句保留；复习排程、评分、撤销及标签接口。
- 真实词句列表、编辑抽屉、草稿恢复、工作台/终端 GUI 收藏入口、语法窗口词句与复习入口、卡片界面。
- JSON 导出/预览导入/冲突副本逻辑与 CSV，正在补全回归和文件选择实测。

## 已有验证证据

- 词句与复习 Rust 回归已运行通过；覆盖迁移、消息复合键、删除来源、重试幂等、冲突、Unicode、排程和撤销等。
- Vue/选区测试已运行通过；覆盖最新草稿 flush、写失败重试、重复收藏草稿保留、迟到搜索结果和答案隔离。
- 实际 Linux Tauri 窗口离线添加词句与注释、保存、正常关闭和重启恢复通过；测试使用独立临时 XDG 数据目录。
- 当前功能已有前端生产构建及阶段性 Linux 调试构建通过，后续改动需要最终重跑。

## 仍需完成与核验

- 导入/导出新增测试全部通过，真实文件选择、取消、覆盖及备份往返验证。
- 实际 GUI 选区收藏、来源保留、释义选段、草稿关闭恢复、420px 语法窗口及复习完整流程。
- 复习/标签/导入等在途操作关闭处理、编辑冲突比较/重载入口、草稿细节、错误重试的完整验收。
- 10,000 条基准数据搜索性能；跨平台 CI 编译与已知真机限制记录。
- 逐条对照原计划做完成审计，修复缺口；更新说明、版本和安装包，执行最终格式/类型/Clippy/测试/原生包验证。

本记录随工作更新；最终是否完成以原计划逐项验收及实际产物为准。
