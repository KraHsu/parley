# M0 验证记录

本次初始化已完成以下本地验证；后续业务功能不在本次验证范围内。

- 当前开发平台：Linux x86_64。
- Node：24.18.0；npm：12.0.1；Rust：1.97.1。
- GTK：3.24.41；WebKitGTK：2.52.6（4.1 API）。
- Codex CLI：沙箱内路径报告 0.154.0，宿主环境的 doctor 报告 0.145.0。当前仅探测版本，未登录或调用模型；M1 必须记录实际选用的可执行路径和版本。
- Windows/macOS：配置 CI，尚未在本次环境执行。

## 已通过

| 检查                                   | 结果                                                                                                                         |
| -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| `npm install` / 锁文件生成             | 成功，安装时 npm audit 报告 0 个已知漏洞                                                                                     |
| `npm run doctor`                       | 宿主环境的 Node、Rust、Cargo、Git、GTK/WebKit 检查通过                                                                       |
| `npm run build`                        | Vue 类型检查与 Vite 生产构建通过                                                                                             |
| `npm run check`                        | Prettier、TypeScript、rustfmt、Clippy（warnings as errors）通过                                                              |
| `npm run desktop:check -- -- --locked` | Linux 桌面调试程序构建与链接通过，生成 `target/debug/parley`                                                                 |
| 浏览器实际检查                         | Chrome 加载界面、目标语言切换为日语、禁用的聊天输入、浏览器 IPC 提示、390px 布局无横向溢出；无未捕获 JS 异常                 |
| 桌面实际检查                           | 在独立 Xvfb 显示器中启动真实 Tauri/WebKit 窗口，使用键盘触发运行环境查询，显示 `Parley 0.1.0 · linux/x86_64 · Rust 连接正常` |

浏览器检查采用临时 CDP 脚本，桌面检查采用独立虚拟显示器和键盘操作；未为静态骨架添加重复实现的测试套件。

## 修正与环境限制

- TypeScript 7.0.2 与本次 vue-tsc 版本的 CLI 加载方式不兼容，已固定到实际构建通过的 5.9.3。
- 沙箱中的 Cargo 网络请求和部分子进程受限，依赖下载及对应检查在授权后于宿主环境完成。
- 虚拟显示器报告 DRI3 加速警告，软件渲染下窗口显示与 IPC 正常。
- 当前只验证调试桌面程序，尚未验证发行安装包、签名、更新、其他平台和真实 Codex 接入。
- 浏览器与桌面截图保存在本次检查的 `/tmp/parley-*-m0.png` 中，未作为项目依赖或测试输入提交。
